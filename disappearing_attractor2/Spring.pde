//class Spring {
  
//  PVector anchor;
  
  //float restLength;  
  //float k = 0.1
  
//  Spring(float x, float y, int 1) {
//    anchor = new PVector(x,y);
//    restLength = 0;
//  }
  
//  void connect(Particle p) {
 
//  PVector spring = PVector.sub(position, origin);
//  float d = force.mag();
//  float stretch = d - restLength;
//  spring.normalize();
//  spring.mult(-1 * k * stretch);
  
//  p.applyForce(spring);
//  }
  
//}
